const elem = document.querySelector("#carousel")
const instance = new Carousel(elem, {initialSlide: 1})  // usage example via instance

// pass the html5 object into new Carousel instance,
// to create a Carousel wrapper above your object
